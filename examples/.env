LOG_LEVEL=INFO
HOSTNAME=host01
FILTER_MODE=https # "https" (default) or "dns"
HTTP_PORT=8080
HTTPS_PORT=8443
POLICY_PATH=/app/policy.yaml

# LOG_FILE="/data/output.log" # Add Volume in docker-compose.yaml for these persistent logs
# DNS_PORT=53
# DNS_UPSTREAMS="127.0.0.11:53"
# DASHBOARD_HOST=http://127.0.0.1:8081
# DASHBOARD_API_KEY=RANDOM_STRING
# NOTIFICATION_HOST=https://gotify.example.com
# NOTIFICATION_KEY=examplekey12345
# NOTIFICATION_IGNORE_DOMAINS=google.com,*.github.com

# Alternative: specify allowlist directly via environment variables (takes precedence over POLICY file)
#ALLOWLIST_IPS=1.1.1.1,8.8.8.8,192.168.0.0/16
#ALLOWLIST_DOMAINS=github.com,*.alpinelinux.org,*.cloudflare.com

# Refer to readme for more configuration options